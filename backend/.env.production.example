# ==================================
# FlowCRM Production Environment
# ==================================

# Application
APP_NAME="FlowCRM"
APP_ENV=production
APP_KEY=base64:YOUR_APP_KEY_HERE
APP_DEBUG=false
APP_URL=https://your-domain.com

# Database
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=crm
DB_USERNAME=crm
DB_PASSWORD=your_secure_password_here

# Cache & Session
CACHE_DRIVER=file
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_DOMAIN=.your-domain.com

# Queue
QUEUE_CONNECTION=database

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your_app_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=your-email@gmail.com
MAIL_FROM_NAME="${APP_NAME}"

# Sanctum (CORS for API Authentication)
SANCTUM_STATEFUL_DOMAINS=your-domain.com,www.your-domain.com

# Logging
LOG_CHANNEL=stack
LOG_LEVEL=error

# Broadcasting (if using)
BROADCAST_DRIVER=log

# Redis (Optional - uncomment if using Redis)
# REDIS_HOST=127.0.0.1
# REDIS_PASSWORD=null
# REDIS_PORT=6379
# REDIS_CLIENT=phpredis
# CACHE_DRIVER=redis
# SESSION_DRIVER=redis
# QUEUE_CONNECTION=redis

# Storage
FILESYSTEM_DISK=local

# Security
# Add these for production security
BCRYPT_ROUNDS=12

# ==================================
# Production Notes:
# ==================================
# 1. APP_KEY: Generate with: php artisan key:generate
# 2. APP_DEBUG: Must be false in production
# 3. DB_PASSWORD: Use strong password (min 16 characters)
# 4. SESSION_DOMAIN: Use your actual domain with leading dot
# 5. MAIL_PASSWORD: For Gmail, use App Password (not account password)
# 6. SANCTUM_STATEFUL_DOMAINS: Add all your frontend domains
# 7. LOG_LEVEL: Use 'error' in production, 'debug' for troubleshooting
#
# Security Checklist:
# ☐ APP_DEBUG=false
# ☐ Strong database password set
# ☐ APP_KEY generated
# ☐ File permissions set (storage: 775, files: 644)
# ☐ HTTPS enabled
# ☐ Firewall configured
# ☐ Regular backups scheduled
