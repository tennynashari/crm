# FlowCRM

Customer & Lead Management System

## Quick Start

```bash
# Setup Backend
cd backend
composer install
php artisan key:generate
php artisan migrate --seed
php artisan serve

# Setup Frontend (new terminal)
cd frontend
npm install
npm run dev
```

Login: admin@flowcrm.test / password

See README.md for full documentation.
